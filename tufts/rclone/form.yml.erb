---
cluster: "pax"

attributes:
  bc_account:
    label: "Project"
    help: "You can leave this blank if **not** in multiple projects."

  partition:
    label: "Partition"
    help: "Select a partition"
    widget: select
    options:
      - ["batch", "batch"]

form:
  - bc_account
  - partition
